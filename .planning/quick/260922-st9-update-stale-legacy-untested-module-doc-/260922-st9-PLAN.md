---
phase: quick-260922-st9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pattern_policy_test.rs
autonomous: true
requirements:
  - QUICK-260922-st9
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low        # no calibration samples for doc-comment-only quick tasks in this repo

must_haves:
  truths:
    - "A reader landing on the empty `LEGACY_UNTESTED` const learns from the comment directly above it that an empty list is MAXIMUM strictness (every pattern must meet 3 positives / 2 negatives, no exemptions), not a disabled check."
    - "The 30 -> 11 -> 0 arc survives in past tense, with #70, #80, #95, #97, #99 and #89 all still named."
    - "The do-not-add-to-this-list instruction survives, and is joined by a do-not-delete-it instruction."
    - "`cargo test --test pattern_policy_test` still passes all 5 tests, unchanged."
    - "The compiled behaviour is byte-identical: every changed line in the diff is a `//!` or `///` doc line."
  artifacts:
    - "tests/pattern_policy_test.rs (modified — doc comments only)"
  key_links:
    - "Module `## The ratchet` section <-> the `LEGACY_UNTESTED` const doc comment: one consistent story, told once, not two overlapping versions."
    - "The three ratchet properties described in prose <-> the three tests below that pin them (`every_pattern_meets_the_test_case_policy`, `the_legacy_exemption_list_does_not_grow`, `a_legacy_pattern_that_now_complies_must_leave_the_list`)."
---

<objective>
Rewrite the two stale doc comments in `tests/pattern_policy_test.rs` so they describe the
`LEGACY_UNTESTED` exemption list as it now is — empty — rather than as the populated debt register
it was before PR #138 (`be0ef9e`, 2026-09-22) backfilled the last 11 ids and closed #89.

Purpose: a reader landing on `const LEGACY_UNTESTED: &[&str] = &[];` under a present-tense comment
that says "they are listed in [`LEGACY_UNTESTED`]" can only conclude one of two wrong things —
either the comment is lying, or the check is disabled. It is the opposite: with no exemptions the
policy is at its strictest, because every shipped pattern must now clear 3 positives / 2 negatives.
The rewrite has to say that plainly enough that nobody later "tidies up" by repopulating the list or
deleting it.

Output: `tests/pattern_policy_test.rs` with two rewritten doc comments and no other change.

No tracer task: this is a documentation-comment-only edit to a single existing file. There is no
stack to slice vertically — the whole change is one layer, and the end-to-end proof (the comment-only
diff shape plus the untouched test run) is carried in the `<verify>` blocks below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@tests/pattern_policy_test.rs

Scope fences the executor must hold:

- The `pattern-library` project skill does NOT apply. No file under `patterns/core/*.yaml` is
  touched, so `docs/PATTERN-CATALOGUE.md` must NOT be regenerated and no schema/recall/corpus gate
  is in play.
- `.claude/skills/code-review/SKILL.md` and `.claude/skills/pr-artifacts/SKILL.md` are the standing
  pre-PR gates for this repo. They run at PR time, not inside this plan.
- Do NOT run the full `cargo test` suite. It takes ~6 minutes and has previously stalled executor
  watchdogs on this repo. The only sanctioned commands are in the `<verify>` blocks.
- Branch is already `docs/legacy-untested-module-doc`, forked off `be0ef9e` on a clean tree. Do not
  create a new branch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Put the module-level "## The ratchet" section in past tense and name the empty list as the end state</name>
  <files>tests/pattern_policy_test.rs</files>
  <precondition>The working tree is clean and `git branch --show-current` reports `docs/legacy-untested-module-doc`; `git merge-base --is-ancestor be0ef9e HEAD` succeeds (the backfill that emptied the list is in history). If any of these is false, halt and report rather than editing.</precondition>
  <action>
Rewrite ONLY the `//!` block that begins at the `## The ratchet` heading (currently lines 16-26).
Leave the first `//!` paragraph (lines 1-14, the #70/#27 origin and the PR #66 PI048 evidence)
byte-identical — it is still accurate and is not in scope.

The rewritten section must carry all six of these points, in the file's existing voice — prose that
explains WHY rather than restating WHAT, wrapped at roughly the same column as the surrounding block
(~76 characters), using `--` as the em-dash form this particular block already uses on its
"as a side effect --" line:

1. The history, in the PAST tense, preserved rather than deleted: 30 of the original patterns
   predated the `assert_positives`/`assert_negatives` helpers and had no cases at all; the four
   category widenings (#80, #95, #97, #99) took that to 11 as a side effect, because rewriting a
   pattern is the natural moment to give it cases; the backfill tracked in #89 covered the last 11
   and emptied the list. Keep every one of those five issue references — they are the audit trail
   for the arc, which is the whole reason the ratchet exists.
2. The state now: `LEGACY_UNTESTED` is empty.
3. The point the reader is most likely to get backwards, stated explicitly and unmissably: an empty
   exemption list is the ratchet at MAXIMUM strictness, not a check that has been switched off.
   With nothing exempt, every shipped pattern has to clear the 3-match / 2-non-match minimum. Make
   this the load-bearing sentence of the section.
4. The empty list is the ratchet's intended END STATE, reached — not a loophole, not a temporary
   condition waiting to be refilled.
5. The three properties, still each pinned by a test below and still meaningful on an empty list:
   a pattern not on the list must comply (which is now every pattern), the list may not grow, and an
   entry that starts complying must be removed from it. Say that the third one is what drove the
   list to empty rather than letting it calcify into a permanent excuse — that is now a completed
   fact, not a hope.
6. Forward guidance in both directions: nothing may be added back (a new pattern without cases gains
   cases, not an exemption), AND the empty const plus its tests stay where they are, because deleting
   them would reopen the exemption route the may-not-grow guard currently holds shut.

Do NOT change `const LEGACY_UNTESTED: &[&str] = &[];`. Do NOT change its `///` doc comment in this
task (that is Task 2). Do NOT touch any `fn`, any `#[test]`, any `assert!` message, or any `//`
comment sitting inside a function body — in particular `the_legacy_exemption_list_does_not_grow`'s
assertion text ("it had 30 entries when #70 landed and must only ever shrink") is a string literal
inside a test, is still accurate as written, and stays byte-identical.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff -U0 -- tests/pattern_policy_test.rs | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[-+]//!' | wc -l | tr -d '[:space:]'</automated>
  </verify>
  <done>
`cargo fmt --all -- --check` exits 0. The second command prints `0`, proving every added and removed
line in the working-tree diff so far is a module-level `//!` doc line — no code, no `///` line, no
in-function comment, no assertion string. All six content points above are present in the rewritten
section, and #70, #80, #95, #97, #99 and #89 are all still named in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh the LEGACY_UNTESTED const doc comment to agree with it, verify, and commit</name>
  <files>tests/pattern_policy_test.rs</files>
  <action>
Rewrite ONLY the `///` doc comment attached to `const LEGACY_UNTESTED` (currently lines 35-39).
Read the section Task 1 produced first and make this comment AGREE with it without restating it —
the module header carries the history and the argument; this comment is the short, local note a
reader sees when they jump straight to the const.

It must:

- Keep the "Do not add to this list." instruction, in bold as it is today, with its reason intact:
  a new pattern without cases must gain cases, not an exemption.
- Say the register is empty because the #89 backfill completed, in the past tense. Do not keep
  "Backfilling is tracked in #89" — #89 is closed, so the present-tense tracking claim is the stale
  half of this comment.
- Keep the still-true mechanic that a test below fails if a compliant id is left behind, and keep
  the `—` em-dash form on that retained sentence rather than normalising it to `--`; that sentence
  already uses `—` and this is not a whitespace-churn change.
- Add the one thing the current comment lacks: the empty const is deliberate and stays. Point at the
  module header for why, rather than duplicating the argument here.

Rewording the opening summary line ("Patterns that shipped before `assert_positives`/
`assert_negatives` existed.") into the past tense is in scope; the const still names that set, the
set is now empty, and the line should read as a description of what the list held rather than what
it holds.

Then run the verification below and commit. Commit message, house convention, one commit:
subject `docs: put the LEGACY_UNTESTED ratchet note in past tense`, body naming PR #138 / `be0ef9e`
as what emptied the list and #89 as closed, and stating that the change is comment-only with no
behaviour change. End the message with the line
`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
Stage `tests/pattern_policy_test.rs` explicitly by path — nothing else.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo test --test pattern_policy_test</automated>
    <automated>grep -cE '^const LEGACY_UNTESTED: &\[&str\] = &\[\];$' tests/pattern_policy_test.rs</automated>
    <automated>git diff -U0 -- tests/pattern_policy_test.rs | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[-+]//[!/]' | wc -l | tr -d '[:space:]'</automated>
    <automated>git show --stat --oneline HEAD | tail -3</automated>
  </verify>
  <done>
`cargo fmt --all -- --check` exits 0. `cargo test --test pattern_policy_test` reports
`5 passed; 0 failed` — `every_pattern_meets_the_test_case_policy`,
`the_legacy_exemption_list_does_not_grow`, `a_legacy_pattern_that_now_complies_must_leave_the_list`,
`every_pi05x_pattern_carries_a_relaxed_pattern`,
`requires_relaxed_pattern_covers_every_category_boundary`. The `grep -cE` prints `1`, proving the
const declaration line is untouched. The diff-shape command prints `0` (run it BEFORE committing, or
re-run it as `git diff -U0 HEAD~1 HEAD -- tests/pattern_policy_test.rs | ...` after), proving every
changed line across both tasks is a `//!` or `///` doc line. `git show --stat` reports exactly
`1 file changed` and names only `tests/pattern_policy_test.rs`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan edits doc comments in a test file. No input parsing, no network, no dependency install, no pattern or detection-logic change, so no trust boundary in this repo moves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-st9-01 | Tampering | `tests/pattern_policy_test.rs` — an edit that widened past comments into the `LEGACY_UNTESTED` const or the three ratchet tests would silently relax the per-pattern test policy, the CI gate from #70 | high | mitigate | Task 2's anchored `grep -cE` pins the const declaration line verbatim, and the diff-shape command fails unless every changed line is a `//!`/`///` doc line. Both are automated, not reviewer-remembered. |
| T-st9-02 | Repudiation | Doc comment accuracy — a comment that misdescribes an empty exemption list as a disabled check invites a future contributor to repopulate or delete it, reopening the exemption route | medium | mitigate | The rewrite is required (module-section point 6, const-doc bullet 4) to carry both a do-not-add and a do-not-delete instruction, so the guidance survives independent of this plan's author. |
| T-st9-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install task in this plan — no dependency is added, removed, or version-changed, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<verification>
1. `cargo fmt --all -- --check` exits 0.
2. `cargo test --test pattern_policy_test` — 5 passed, 0 failed.
3. The full change diff contains only `//!` and `///` lines (diff-shape command prints `0`).
4. `const LEGACY_UNTESTED: &[&str] = &[];` is present and byte-identical (anchored grep prints `1`).
5. Exactly one file changed, in exactly one commit, on branch `docs/legacy-untested-module-doc`.

Explicitly NOT run: the full `cargo test` suite (~6 min, watchdog stall risk), the GATE-03 sweep,
`docs/PATTERN-CATALOGUE.md` regeneration, and any recall/corpus gate. None of them can move, because
no pattern, no engine and no test logic changes.
</verification>

<success_criteria>
- The `## The ratchet` section reads entirely in the past tense for the history and the present tense
  only for the current, empty state.
- A reader who jumps to `const LEGACY_UNTESTED: &[&str] = &[];` is told by the comment above it that
  empty means every pattern is enforced, and cannot mistake it for a disabled check.
- #70, #80, #95, #97, #99 and #89 all still appear in the file.
- Both a do-not-repopulate and a do-not-delete instruction are present.
- Compiled behaviour is byte-identical — proven by the diff-shape check, not asserted.
</success_criteria>

<output>
Create `.planning/quick/260922-st9-update-stale-legacy-untested-module-doc-/260922-st9-SUMMARY.md` when done.
</output>
