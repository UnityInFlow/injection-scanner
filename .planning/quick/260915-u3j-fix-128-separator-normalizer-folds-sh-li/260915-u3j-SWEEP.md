# GATE-03 sweep — quick task 260915-u3j (issue #128)

## What was run

- **Before:** `.planning/local/sweep-before-u3j-2026-09-16/`, built from the working tree at
  the fork point (`27e4d49` — HEAD's own commits since then touch only planning docs, so
  `git status --porcelain -- src patterns tests` was empty and the release binary built from
  that state is the correct pre-edit baseline).
- **After:** `.planning/local/sweep-after-u3j-2026-09-16/`, built from the working tree after
  Task 1 (`66351c8`) and Task 2 (`27df0ff`) — the full manufactured-boundary gate.
- **Directory list:** the identical 32-entry array used by the Phase 4 sweeps (read via
  `cut -f1` on `.planning/local/sweep-after-04-review-fixes-2026-09-07/manifest.tsv`, no
  word-splitting), passed unmodified to both runs.
- **Binary:** `target/release/injection-scanner`, rebuilt for each run
  (`cargo build --release`, backgrounded per P0).

## Manifest and summary — byte-identical

```
diff sweep-before/manifest.tsv sweep-after/manifest.tsv   -> exit 0, no output
diff sweep-before/summary.tsv  sweep-after/summary.tsv    -> exit 0, no output
```

Totals over the swept corpus (both runs): **23,037 files, 518 findings.** Every
per-directory row (file count, finding count, skip count) is identical between the two
runs — see the committed `manifest.tsv` / `summary.tsv` in `sweep-before/` and
`sweep-after/` alongside this file.

## `--compare`, both directions

Run against `.planning/local/sweep-before-u3j-2026-09-16/` and
`.planning/local/sweep-after-u3j-2026-09-16/` — **never** against this repository's own
committed sweep directories under `.planning/phases/` or `.planning/quick/`, which hold
zero `*.json` by design and would silently load an empty baseline (the recorded trap in
STATE.md and in the plan's `<gate_procedures>`).

```
$ bash scripts/gate03-sweep.sh --compare sweep-before-u3j-2026-09-16 sweep-after-u3j-2026-09-16
(no output)
APPEARANCES_EXIT=0

$ bash scripts/gate03-sweep.sh --compare sweep-after-u3j-2026-09-16 sweep-before-u3j-2026-09-16
(no output)
DISAPPEARANCES_EXIT=0
```

Both directions returned an **empty** diff set and exit code `0`.

## Adjudication

- **DISAPPEARANCES: none.** Zero findings present in the before-sweep and absent from the
  after-sweep. This is the expected *shape* (disappearances-only), realized here as the
  empty case: none of the 23,037 real third-party files this corpus reaches (GSD tooling
  and skills directories, sibling UnityInFlow repos, VS Code / Warp / kiro / Codex config
  and cache trees, two unrelated personal repos) happen to contain the specific compound
  tokens the six issue-#128 false positives are built from (`sh-lint`, `sh-report`,
  `sh-runner`, `on-call`, `DAN-mode-switch`) at a PI028/PI030/PI031 match edge. The six
  false positives were reproduced directly against the scanner in
  `tests/manufactured_boundary_test.rs` (Task 2) and in the targeted probes recorded in
  `docs/adr/ADR-006-manufactured-boundary-gate.md` — this sweep's job was to confirm the
  gate does not touch anything ELSE in a large, real, uncurated corpus, and it did not.
- **APPEARANCES: none.** Zero findings present in the after-sweep and absent from the
  before-sweep. The one appearance shape the plan pre-empts — a raw-pass artefact that was
  dedup-blocking a genuinely different normalized-pass match, now able to fire once the
  artefact is filed separately — did not occur in this corpus either; there is nothing to
  adjudicate as "confirmed genuine" because the set is empty.
- **Verdict: PASS.** Both directions are empty, which satisfies "disappearances only" as a
  vacuous case and produces no red flag (no appearances at all, let alone an unexplained
  one). Combined with the full targeted-test evidence in Task 2 and the eleven-pattern audit
  in ADR-006, the gate's behavior is proven both on the six known false positives (fixed) and
  on 23,037 real files it must not otherwise disturb (unchanged).

## GATE-02, run alongside (not a GATE-03 artifact, recorded for completeness)

`cargo test --test recall_test` — green, `EXPECTED` byte-identical to `27e4d49`
(`git diff --quiet 27e4d49 -- tests/recall_test.rs` and `-- README.md` both exit 0, checked
before Task 4's README edits were made).
