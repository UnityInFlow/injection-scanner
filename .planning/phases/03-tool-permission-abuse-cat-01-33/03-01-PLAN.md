---
phase: 03-tool-permission-abuse-cat-01-33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/corpus/attack/structural/README.md
  - tests/corpus/attack/structural/01-wildcard-allowed-tools-block-sequence.md
  - tests/corpus/attack/structural/*.md
  - tests/corpus/attack/tool-permission-abuse.md
  - tests/corpus/attack/README.md
  - tests/recall_test.rs
  - README.md
autonomous: true
requirements: [CAT-01, GATE-01, GATE-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The recall harness scans whole-file, `---`-fenced frontmatter payloads as single documents, not line-split (D-01)."
    - "A structural payload that fails to parse as frontmatter fails the build loudly instead of reading as an undetected miss (D-05, trap 2)."
    - "A structural finding is provably reachable end-to-end from a corpus payload when a frontmatter-scoped pattern is loaded (trap 6)."
    - "The 12 CAT-01 payloads are measured against the current 48-pattern binary before any PI05x exists, and that number is committed (D-04)."
    - "Recall is pinned exactly on two separate rows so the structural half cannot silently go to zero (D-02)."
  artifacts:
    - "tests/corpus/attack/structural/ — whole-file payload directory with its own README.md"
    - "tests/corpus/attack/tool-permission-abuse.md — line-oriented prose payloads"
    - "tests/recall_test.rs — structural_payloads(), measure_structural(), two new EXPECTED rows, probe test"
  key_links:
    - "structural/ directory -> the new collector (NOT categories(), which filters p.is_file() and would drop it silently)"
    - "EXPECTED's tool-permission-abuse-structural row -> every_claimed_category_has_a_corpus_file, which must consult both collectors"
    - "structural payload text -> frontmatter::analyze() -> ProjectedLine::render() -> a frontmatter-scoped pattern"
---

<objective>
Land the CAT-01 corpus and the harness that can measure it, and commit the measured
pre-pattern baseline. **No PI05x pattern exists at the end of this plan — that is the point.**

Purpose: D-04 makes GATE-01's ordering visible in git history rather than asserted. The 12 payloads
are written from the threat model and scored against the shipping 48-pattern binary first; the
patterns land later and the delta is the evidence. A plan that wrote patterns and corpus together
would destroy GATE-01's only enforcement mechanism.

Output: 12 threat-model payloads (prose file + `structural/` directory), a second collection mode in
`tests/recall_test.rs` that can actually see them, two exactly-pinned `EXPECTED` rows carrying the
baseline, and the README recall table updated with them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/.continue-here.md
@.planning/phases/03-tool-permission-abuse-cat-01-33/03-CONTEXT.md
@.planning/phases/03-tool-permission-abuse-cat-01-33/03-RESEARCH.md
@.planning/phases/03-tool-permission-abuse-cat-01-33/03-PATTERNS.md

@tests/recall_test.rs
@tests/corpus/attack/README.md
@src/frontmatter.rs
@src/pattern.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a frontmatter payload is measured" — one payload only</name>
  <files>tests/corpus/attack/structural/README.md, tests/corpus/attack/structural/01-wildcard-allowed-tools-block-sequence.md, tests/recall_test.rs</files>
  <read_first>
    - `tests/recall_test.rs` in full — `EXPECTED`, `payloads()`, `categories()`, `detected()`, `measure()`, and the three `#[test]` functions. This is the file being modified.
    - `src/frontmatter.rs` lines 100-180 — `ProjectedLine::render`, `extract`, `extract_delimited`. `extract_delimited` requires the fence on the document's LITERAL FIRST LINE (`lines.next()`), which is the constraint the payload format has to obey.
    - `src/scanner.rs` lines 455-500 — the structural pass and the short-circuit that makes it inert when no frontmatter-scoped pattern is loaded.
    - `src/pattern.rs` lines 30-115 — `PatternScope`, the `Pattern` struct, `#[serde(deny_unknown_fields)]`.
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-PATTERNS.md` §`tests/recall_test.rs` — the three exact anchor blocks to edit.
  </read_first>
  <action>
    Wire ONE structural payload through every layer this phase touches — corpus file, collector,
    frontmatter parser, projection, frontmatter-scoped matching, recall pin — and stop there. No
    second payload, no prose file, no shipped pattern.

    Create `tests/corpus/attack/structural/` containing exactly two files.

    (a) `README.md` — the directory's own contract, following the register of
    `tests/corpus/attack/README.md`. It must state: one file is one whole payload scanned as a
    single document (not line-split, which is why this directory exists — `payloads()` splits on
    newlines and cannot express a frontmatter payload at all, D-01); the opening fence must be the
    file's literal first line because `extract_delimited` reads it with `lines.next()`; therefore
    per-file rationale lives in this README or after the closing fence, never before the opening
    one; and GATE-01's sourcing rule applies unchanged — payloads come from the threat model and
    issue #33, never from a regex. This README is excluded from collection by name, exactly as
    `categories()` excludes the top-level one.

    (b) `01-wildcard-allowed-tools-block-sequence.md` — the tracer payload. A realistic Claude Code
    skill file whose YAML frontmatter grants tools via a **block sequence** (the array form real
    skills use, per 03-RESEARCH.md Pitfall 1 — it projects as `allowed-tools[0] = ...`,
    `allowed-tools[1] = ...`, never as one `allowed-tools = *` line), one entry of which is an
    unrestricted shell grant, plus a plausible skill body. Written from the threat model and issue
    #33's first bullet. The literal first line of the file is the opening fence.

    In `tests/recall_test.rs` add, beside the existing collectors and without altering them:
    - `structural_dir()` — `attack_dir().join("structural")`.
    - `structural_payloads()` — every file directly in `structural_dir()` except `README.md`,
      sorted, each read whole with `fs::read_to_string`. Returns `(PathBuf, String)` pairs. Must NOT
      call `payloads()`; the line splitter is what D-01 exists to bypass. Add a doc comment stating
      that `categories()`'s `p.is_file()` filter drops this directory silently (D-05) and that CAT-02
      (#34) will need either its own subdirectory or a generalisation of this function — flat for now,
      per D-01's literal path.
    - `measure_structural()` — the structural analogue of `measure()`, reusing `detected()` but
      passing each payload's real file name so failure output names the file, and scanning the whole
      string as one document.
    - A `STRUCTURAL_CATEGORY` constant holding `tool-permission-abuse-structural`.

    Add the structural row to `EXPECTED` with total 1 and detected 0, carrying a comment in the
    register of the existing rows: measured on the shipping 48-pattern set, zero by construction
    because no pattern declares `scope: frontmatter` yet, and this row exists so the structural half
    cannot silently go to zero later (D-02).

    Extend `recall_matches_the_recorded_numbers` to fold the structural measurement in as an extra
    row, and extend `every_claimed_category_has_a_corpus_file` so a name in `EXPECTED` may be
    satisfied by EITHER a top-level category file OR the structural collector — otherwise the new
    row makes that test fail against a corpus that is actually present.

    Add three new tests:
    - `the_structural_corpus_is_actually_collected` — asserts `structural_payloads()` is non-empty
      and that its count equals the structural row's total in `EXPECTED`. This is D-05's explicit
      requirement; the exact-pin alone is necessary but not sufficient.
    - `every_structural_payload_parses_as_frontmatter` — for each payload asserts
      `injection_scanner::frontmatter::analyze(&content)` returns `Ok(Some(_))` with at least one
      projected line, and names the file in the failure message with the fence-on-line-1 reason.
      This is the guard against trap 2: a leading rationale comment would make the payload
      unparseable and read as a detection miss rather than a corpus bug.
    - `the_structural_pass_is_reachable_from_the_corpus` — the arming control. Build a probe
      category by parsing a small YAML literal through `serde_yaml::from_str::<PatternCategory>`
      (parsed, not struct-literal, so a later schema field addition cannot break it), containing one
      generic `scope: frontmatter` probe keyed on the projected key name — NOT a shipped pattern and
      never added to `patterns/`. Assert the probe scanner reports at least one match on at least
      one structural payload, AND assert the same probe with `scope` left at its prose default
      reports zero on the same payload. The second half is the positive control the
      `.continue-here.md` blocking anti-pattern demands: without it, a silent structural pass and a
      working one are indistinguishable.
  </action>
  <acceptance_criteria>
    - `head -1 tests/corpus/attack/structural/01-wildcard-allowed-tools-block-sequence.md` prints exactly `---`.
    - `ls tests/corpus/attack/structural/ | grep -v README | wc -l` prints `1`.
    - `cargo test --test recall_test the_structural_corpus_is_actually_collected` passes, and temporarily renaming `tests/corpus/attack/structural` makes it FAIL (mutation check — run it, then restore).
    - `cargo test --test recall_test every_structural_payload_parses_as_frontmatter` passes; inserting a `#` comment line above the opening fence of the payload makes it FAIL (mutation check — run it, then restore).
    - `cargo test --test recall_test the_structural_pass_is_reachable_from_the_corpus` passes, proving both that the frontmatter-scoped probe fires and that the prose-scoped variant of the same probe does not.
    - `grep -c 'tool-permission-abuse-structural' tests/recall_test.rs` is at least 2 (the `EXPECTED` row and the constant).
    - `cargo test --test recall_test` is fully green, including `every_claimed_category_has_a_corpus_file` with the new row present.
    - `grep -rn 'scope:' patterns/core/*.yaml` still returns nothing — no pattern ships in this task.
    - `cargo fmt --all -- --check` and `cargo clippy --all-targets --locked -- -D warnings` both pass.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test recall_test && cargo fmt --all -- --check && cargo clippy --all-targets --locked -- -D warnings</automated>
  </verify>
  <done>One realistic frontmatter payload travels corpus file -> whole-file collector -> `frontmatter::analyze` -> projection -> a frontmatter-scoped probe -> a reported match -> an exactly-pinned recall row, with a positive control proving the finding came from the structural pass and not from raw-line matching. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining 11 threat-model payloads and the corpus README correction</name>
  <files>tests/corpus/attack/structural/NN-*.md, tests/corpus/attack/tool-permission-abuse.md, tests/corpus/attack/README.md, tests/recall_test.rs</files>
  <read_first>
    - `tests/corpus/attack/README.md` in full — the sourcing rule and the paragraph naming tool and permission abuse as deliberately absent, which this task must correct.
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-RESEARCH.md` Appendix "Threat Model Catalogue" — the sourced attack shapes, their confidence tiers, and the two explicitly-admitted phrasing gaps.
    - GitHub issue #33's pattern list, reproduced in `docs/DETECTION-BACKLOG.md` §`PI050`-`PI059`.
    - `tests/corpus/attack/role-override.md` — the line-oriented format and comment register to match.
    - `tests/recall_test.rs::payloads` — the parser that defines the prose file's format: non-blank, non-`#`-prefixed lines are payloads, one payload per line, each scanned as its own document.
  </read_first>
  <action>
    Write the remaining 11 payloads so the category totals exactly 12 (GATE-01), all drawn from the
    threat model, issue #33 and the research appendix — never from a regex, and no PI05x regex exists
    yet to derive them from.

    Do NOT set a prose/structural ratio in advance (D-03). Write each payload in whichever form the
    attack actually takes, then record the split that results in the plan SUMMARY. A split chosen up
    front shapes the corpus by the implementation plan, which is the same defect GATE-01 exists to
    prevent.

    Structural payloads go in `tests/corpus/attack/structural/` as further `NN-descriptive-name.md`
    files, each a whole realistic document with the fence on line 1. Cover the shapes the research
    names: the scalar wildcard form, the JSON manifest form (a whole-file JSON document with no
    fence — `extract` handles that case and it is the highest-value input, per its doc comment), a
    `permissions.allow` wildcard grant in a settings-shaped document, and a structurally-set
    bypass permission mode. Vary the surrounding document so the corpus is not twelve dressings of
    one shape.

    Prose payloads go in a new `tests/corpus/attack/tool-permission-abuse.md`, one per line, opening
    with a `#` comment block in the register of the existing category files that states the sourcing
    and names the research appendix's two admitted gaps. Cover: the permission-widening CLI flags,
    an unrestricted-authority claim, a skip-confirmation directive, a persuasion to write a widening
    entry into a settings file, and the imperative disable-a-guardrail form that D-17 keeps in
    CAT-01. Do NOT write payloads for the items D-14 drops from this category (bare dangerous
    commands) or D-15's pipe-to-shell gap — both are out of scope under GATE-04 and would land in
    the wrong category's measurement.

    Update `EXPECTED`'s two rows to the new totals (structural total and prose total summing to 12);
    leave the detected counts at whatever Task 3 measures. Add the prose row with a comment in the
    existing register.

    Correct `tests/corpus/attack/README.md`: the paragraph listing tool and permission abuse as
    deliberately absent is now false, and that file's own instruction is to update it in the same
    commit that adds the payloads. Replace it with the two remaining absent categories and add a
    short paragraph describing the `structural/` layout and why it is a second collection mode.

    Extend `no_payload_is_duplicated_across_the_corpus` to include structural payloads as
    whole-file strings, so a copy-pasted payload cannot pad the twelve.
  </action>
  <acceptance_criteria>
    - Payload total is exactly 12: `ls tests/corpus/attack/structural/ | grep -v README | wc -l` plus the non-blank non-`#` line count of `tests/corpus/attack/tool-permission-abuse.md` equals 12.
    - The two `EXPECTED` totals sum to 12, asserted by a test in `tests/recall_test.rs` that reads both rows.
    - `for f in tests/corpus/attack/structural/[0-9]*; do head -1 "$f"; done` prints only `---` or `{`-opening lines — every structural payload's config block starts on its literal first line.
    - `cargo test --test recall_test every_structural_payload_parses_as_frontmatter` passes for all structural payloads, not just the tracer's.
    - At least one structural payload uses the YAML block-sequence form and at least one is a whole-file JSON document, verified by `grep -l '^{' tests/corpus/attack/structural/*` returning at least one file.
    - `grep -ni 'deliberately' tests/corpus/attack/README.md` no longer lists tool and permission abuse among the absent categories.
    - `cargo test --test recall_test no_payload_is_duplicated_across_the_corpus` passes and covers structural payloads.
    - `grep -rn 'scope:' patterns/core/*.yaml` still returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test recall_test && cargo fmt --all -- --check</automated>
  </verify>
  <done>Twelve threat-model payloads exist across the prose file and `structural/`, the split is whatever the threat model produced rather than a target, and the attack corpus README no longer claims this category is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the baseline against the shipping 48-pattern set and pin it</name>
  <files>tests/recall_test.rs, README.md</files>
  <read_first>
    - `tests/recall_test.rs` as edited by Tasks 1 and 2 — `EXPECTED` and the failure output that names missed payloads and detected counts.
    - `README.md` lines 255-330 — the pattern-count sentence, the category table, the recall table, and the "What this still is not" paragraph naming tool and permission abuse as having no patterns at all.
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-CONTEXT.md` D-04 and the §Specific Ideas note requiring incidental hits to be named by pattern id, not reported as a bare count.
  </read_first>
  <action>
    Measure, do not predict. Run the recall suite and read the actual detected counts for both new
    rows off the failure output, then pin them in `EXPECTED`. Any non-zero count is spillover from
    the existing 48 patterns and must be attributed: for each detected payload, record which pattern
    id fired, by scanning that payload with the release binary in JSON mode. A bare count is not
    interpretable; a count with pattern ids attached makes the later delta interpretable.

    Record in the plan SUMMARY: the structural row baseline (expected zero, because no pattern
    declares `scope: frontmatter` and `scanner.rs` short-circuits the whole pass — if it is non-zero,
    stop and investigate, that would mean the short-circuit is not doing what its comment says), the
    prose row baseline, the prose/structural split (D-03), and every spillover pattern id with the
    payload it fired on.

    Update the README recall table in the same commit (GATE-02's two-place rule): add one combined
    `Tool & Permission Abuse` row carrying the baseline detected count over 12, update the total row
    to the new denominator of 72, and add one italic line stating that the payloads landed before the
    patterns deliberately, per GATE-01, and that the row is the pre-pattern baseline. Correct the
    "What this still is not" paragraph: tool and permission abuse is no longer absent from the
    corpus, though it still has no patterns at this commit. Leave the pattern-count sentence and the
    category table untouched — no pattern changed.

    Commit the corpus, harness and baseline together as one commit whose message states the baseline
    number, so the pattern commit's delta is a readable diff rather than a claim.
  </action>
  <acceptance_criteria>
    - `cargo test --test recall_test` is green with `EXPECTED` carrying measured, not guessed, numbers.
    - The structural row's detected count is `0`; if it is not, the plan SUMMARY records why and the discrepancy is investigated before commit.
    - `grep -c '72' README.md` is at least 1 and the README recall table's total row denominator reads 72.
    - The README recall table contains a `Tool & Permission Abuse` row whose numerator equals the sum of the two `EXPECTED` detected counts.
    - The plan SUMMARY names every spillover pattern id with the payload it fired on, or states explicitly that the baseline was zero across both rows.
    - `cargo test --locked` is fully green (full suite, not just recall) — confirms nothing else in the 331-test suite reads the attack corpus in a way the new directory broke.
    - `git log -1 --format=%s` names the measured baseline.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked</automated>
  </verify>
  <done>The 12 payloads are committed with their measured pre-pattern score pinned in both `EXPECTED` and the README, and the spillover is attributed by pattern id. GATE-01's ordering is now a fact in git history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus file -> `frontmatter::extract`/`parse` | Attacker-shaped YAML/TOML/JSON enters the bounded parser; this is the same untrusted-input path a real scan takes |
| corpus file -> recall harness | Test data governs a published security claim; a harness that silently measures nothing is a claim with no evidence behind it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Repudiation | `tests/recall_test.rs::categories()` `p.is_file()` filter | high | mitigate | Second collector plus `the_structural_corpus_is_actually_collected`, mutation-checked by renaming the directory (Task 1) |
| T-03-02 | Repudiation | structural payload fence position | high | mitigate | `every_structural_payload_parses_as_frontmatter`, mutation-checked by inserting a leading comment (Task 1) |
| T-03-03 | Repudiation | inert structural pass (`scanner.rs` short-circuit) | high | mitigate | `the_structural_pass_is_reachable_from_the_corpus` arms the pass with a parsed probe and carries a prose-scoped negative control (Task 1) |
| T-03-04 | Denial of Service | `frontmatter::parse` on attacker-authored corpus YAML | low | accept | `MAX_DEPTH`/`MAX_NODES`/`MAX_VALUE_LEN` already bound this (ENG-01); this plan adds no parsing surface |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed in this plan; `Cargo.toml` and `Cargo.lock` are unchanged, asserted by `git diff --stat Cargo.toml Cargo.lock` being empty at commit |
</threat_model>

<verification>
- `cargo test --locked` green.
- `cargo fmt --all -- --check` and `cargo clippy --all-targets --locked -- -D warnings` clean.
- `grep -rn 'scope:' patterns/core/*.yaml` empty — this plan ships zero patterns (D-04).
- Both directory-rename and leading-comment mutation checks performed and restored.
</verification>

<success_criteria>
12 threat-model payloads committed and measurable; the structural half provably collected, provably
parseable, and provably reachable; both `EXPECTED` rows and the README carrying the measured
pre-pattern baseline; zero patterns shipped.
</success_criteria>

## Artifacts this phase produces

The complete cross-plan artifact list for Phase 3. Plans 02-07 produce the rows attributed to them.

| Artifact | Kind | Plan |
|---|---|---|
| `tests/corpus/attack/structural/` (directory) | corpus | 01 |
| `tests/corpus/attack/structural/README.md` | corpus doc | 01 |
| `tests/corpus/attack/structural/NN-*.md` (structural payloads) | corpus | 01 |
| `tests/corpus/attack/tool-permission-abuse.md` | corpus | 01 |
| `structural_dir()`, `structural_payloads()`, `measure_structural()`, `STRUCTURAL_CATEGORY` in `tests/recall_test.rs` | fn/const | 01 |
| `the_structural_corpus_is_actually_collected` | test | 01 |
| `every_structural_payload_parses_as_frontmatter` | test | 01 |
| `the_structural_pass_is_reachable_from_the_corpus` | test | 01 |
| `EXPECTED` rows `tool-permission-abuse`, `tool-permission-abuse-structural` | data | 01 |
| `tests/corpus/clean/sandbox-bypass-runbook.md` | corpus | 02 |
| `tests/corpus/clean/settings-permissions-reference.md` | corpus | 02 |
| `tests/corpus/clean/narrow-allowed-tools-skill.md` | corpus | 02 |
| `tests/corpus/clean/settings-deny-list.md` | corpus | 02 |
| `tests/corpus/clean/mcp-setup-guide.md` | corpus | 02 |
| `scripts/gate03-sweep.sh` | script | 03 |
| `.planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md` | evidence | 03, 07 |
| `Pattern::relaxed_pattern` field in `src/pattern.rs` | schema field | 04 |
| `tests/pattern_relaxed_control_test.rs` | test file | 04 |
| `every_pi05x_pattern_carries_a_relaxed_pattern` in `tests/pattern_policy_test.rs` | test | 04 |
| `patterns/core/tool-permission-abuse.yaml` (category `tool_permission_abuse`) | pattern file | 05 |
| `TOOL_PERMISSION_ABUSE_YAML` const + `yamls` entry in `src/patterns/mod.rs` | registration | 05 |
| `PI050` wildcard-tool-grant (CRITICAL, `scope: frontmatter`) | pattern | 05 |
| `PI051` wildcard-permission-allow (CRITICAL, `scope: frontmatter`) | pattern | 05 |
| `PI052` bypass-permission-mode (CRITICAL, `scope: frontmatter`) | pattern | 05 |
| `PI053` skip-permissions-flag (HIGH, prose) | pattern | 06 |
| `PI054` unrestricted-permission-grant (HIGH, prose) | pattern | 06 |
| `PI055` skip-confirmation-directive (HIGH, prose) | pattern | 06 |
| `PI056` widen-settings-directive (HIGH, prose) | pattern | 06 |
| `PI057` disable-guardrail-directive (HIGH, prose) | pattern | 06 |

Pattern names are consumer contracts (`pattern_name` ships in the JSON `spec-ci-plugin` reads).
Fix them at the ids above; widen a `description` later, never rename. The final pattern count is
recorded after the fact per D-16 — the ids above are the planned set, not a quota.

<output>
Create `.planning/phases/03-tool-permission-abuse-cat-01-33/03-01-SUMMARY.md` when done, recording
the prose/structural split, the measured baseline for both rows, and every spillover pattern id.
</output>
