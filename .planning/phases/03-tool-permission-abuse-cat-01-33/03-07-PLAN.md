---
phase: 03-tool-permission-abuse-cat-01-33
plan: 07
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - .planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md
  - README.md
  - docs/DETECTION-BACKLOG.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CAT-01, GATE-02, GATE-03, GATE-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The patterns were swept against real third-party documentation with the RELEASE binary, as a delta against Plan 03's recorded pre-pattern baseline over the same directory set (GATE-03)."
    - "The whole-repo self-scan is clean outside `examples/`, `patterns/`, `tests/`, `tools/`, save for the two findings that predate this milestone."
    - "Every number this phase publishes agrees with every other place it is published."
    - "Everything deliberately deferred has an issue, not a paragraph."
  artifacts:
    - ".planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md — after-sweep section filled with the delta"
    - "docs/DETECTION-BACKLOG.md — the deferred CAT-01 items recorded"
  key_links:
    - "scripts/gate03-sweep.sh --compare -> the before/after delta that is GATE-03's actual evidence"
    - "the sweep -> the ENG-02 class of defect that only real bytes surface, which 16 green unit tests missed"
---

<objective>
Close the phase: sweep the new patterns against real third-party documentation as a delta, prove the
repo self-scan is clean, reconcile every published number, file the deferrals, and run the pre-PR
gate.

Purpose: GATE-03 is the gate that caught a production panic 16 green decoder unit tests missed — a
fixed-offset byte slice that crashed on a multi-byte character in this repo's own source. Unit tests
substituting for the sweep is a recorded blocking anti-pattern on this milestone. This is also the
last point at which a number published in the README, the catalogue, the recall test and the
CHANGELOG can be caught disagreeing with the others.

Output: the after-sweep delta recorded against a named directory set, a reconciled set of published
numbers, issues filed for every deferral, and a green pre-PR gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tool-permission-abuse-cat-01-33/03-CONTEXT.md
@.planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md
@.planning/phases/03-tool-permission-abuse-cat-01-33/03-06-SUMMARY.md
@.planning/.continue-here.md

@.claude/skills/pattern-library/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The GATE-03 after-sweep, as a delta, with the release binary</name>
  <files>.planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md</files>
  <precondition>The same third-party directories Plan 03 enumerated in `03-SWEEP.md` are still present and readable on this machine, and `/tmp/gate03-baseline` still holds Plan 03's baseline output. If the baseline output directory is gone, re-run Plan 03's sweep command at the pre-pattern commit SHA recorded in `03-SWEEP.md` before proceeding — the delta is meaningless against a re-derived set.</precondition>
  <read_first>
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md` — the recorded directory list, per-directory file counts, the pre-pattern git SHA, the exact reproduction command line, the pre-existing findings, and the empty section reserved for this task.
    - `scripts/gate03-sweep.sh` — its two modes and its output layout.
    - `.claude/skills/pattern-library/SKILL.md` §"Scan the whole repo, not just the corpus" — the self-scan one-liner and the `[]` expectation, with its exclusion set.
    - `.planning/.continue-here.md` §"Critical anti-patterns" row one — why this must be the release binary over real directories rather than `cargo test`.
  </read_first>
  <action>
    Build the release binary from the current HEAD, then re-run the sweep over the EXACT directory
    list recorded in `03-SWEEP.md` — not a re-discovered list, since a different set makes the delta
    uninterpretable — writing into `/tmp/gate03-after`. Then run the script's compare mode with the
    baseline directory first and the after directory second.

    Every finding the compare mode reports is new, caused by this phase's patterns, and must be
    triaged individually: name the file, the line, the pattern id and the verdict. A verdict is one
    of three things and nothing else — a true positive on a document that really does widen an
    agent's authority, which is the tool working and should be celebrated in the SUMMARY; a false
    positive, which means the pattern is narrowed or dropped before merge and this plan blocks until
    it is; or a finding inside this repository's own corpora, which is expected and is why the
    code-scanning baseline keeps them in scope rather than excluding them.

    Separately, run the whole-repo self-scan with the release binary using the pattern-library
    skill's own one-liner, which excludes `examples/`, `patterns/`, `tests/` and `tools/`. The
    expectation is an empty list. Two findings in `docs/PATTERN-CATALOGUE.md` are already known to
    predate this milestone and are recorded in Plan 03's baseline; anything beyond those two is
    introduced by this phase and must be fixed. If a new finding is documentation that needs a code
    span, apply the code span — that is the remediation this tool itself prints — rather than
    weakening the pattern.

    Watch for a crash rather than a finding. The scanner's exit codes 0, 1 and 2 are all scan
    verdicts; anything else is the binary crashing on real bytes, which is exactly the class of
    defect this sweep exists to surface and which no unit test in this repository would catch. If
    the binary crashes, stop and treat it as blocking.

    Fill the reserved after-sweep section of `03-SWEEP.md`: the post-pattern git SHA, the same
    directory list with its file counts, the after `summary.tsv` as a markdown table, the compare
    output in full, the triage verdict for every new finding, and the self-scan result. State the
    total files swept and whether it matches the baseline run's total; a different total means the
    machine-local corpus moved and the delta is weaker evidence, which must be said rather than
    glossed.
  </action>
  <acceptance_criteria>
    - `bash scripts/gate03-sweep.sh /tmp/gate03-after <the directory list from 03-SWEEP.md>` completes with the release binary and writes `manifest.tsv` and `summary.tsv`.
    - `bash scripts/gate03-sweep.sh --compare /tmp/gate03-baseline /tmp/gate03-after` was run and its full output is pasted into `03-SWEEP.md`.
    - Every finding in that compare output carries a written verdict of true positive, false positive, or in-repo corpus — none is left untriaged.
    - Zero findings are triaged as false positives at merge time; any that were have been narrowed or dropped in a recorded follow-up commit, and the compare was re-run afterwards.
    - The self-scan one-liner from the pattern-library skill returns a list containing at most the two known pre-existing `docs/PATTERN-CATALOGUE.md` findings, and the SUMMARY names them.
    - The scanner exited 0, 1 or 2 on every directory; any other exit code is recorded as blocking and the plan does not complete.
    - `03-SWEEP.md`'s after-sweep section states the post-pattern SHA, the per-directory file counts and the total, and whether that total matches the baseline run.
    - The directory list used matches the one recorded in `03-SWEEP.md` literally.
  </acceptance_criteria>
  <verify>
    <automated>test -s /tmp/gate03-after/summary.tsv && grep -q 'after-sweep' .planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md</automated>
  </verify>
  <done>The new patterns have met real third-party bytes through the release binary, every new finding is triaged, the repo self-scan is clean beyond the two known pre-existing findings, and the whole thing is recorded as a delta rather than as a claim.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure performance and reconcile every published number</name>
  <files>README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `tests/perf_regression_test.rs` — the pinned budget and what it measures.
    - `README.md` line 597 and its surrounding section — the measured scan time and the 200ms budget.
    - `.planning/REQUIREMENTS.md` §"Out of Scope", the Aho-Corasick row — it cites a headroom figure that research already measured as stale.
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-RESEARCH.md` §Pitfall 4 — the stale-headroom finding, and this repo's established practice that every category widening measures and reports its own cost.
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-06-SUMMARY.md` — the final pattern count and both recall rows.
    - `tests/corpus/attack/README.md`, `docs/PATTERN-CATALOGUE.md`, `CHANGELOG.md`, `tests/recall_test.rs` — the four other places this phase's numbers appear.
  </read_first>
  <action>
    Measure the scan time on the same shape of input the README's figure describes, with the release
    binary, and report the new number and the headroom against the 200ms budget. Eight new patterns
    plus a structural pass that now actually runs — it was short-circuited before this phase, because
    no pattern declared frontmatter scope — is a real cost that was previously zero, so measure it
    rather than assuming it is negligible. Update the README figure if it moved, and state the new
    headroom as a multiple. Do not reason from the stale figure in REQUIREMENTS.md; correct that row
    too, with the measured number and a note that it predates both ENG-02 and this phase.

    Then reconcile. Assemble every number this phase publishes and check each against the others:
    the total pattern count, which appears in the README's pattern-count sentence, the README
    category table, the generated catalogue's count line and `test_total_pattern_count`; the CAT-01
    recall figures, which appear in both `EXPECTED` rows, the README recall table and the CHANGELOG;
    the corpus payload total of 12, which appears in the two `EXPECTED` totals and the attack corpus
    README; the clean corpus specimen count, which appears in the pattern-library skill; and the
    recall denominator of 72. Any disagreement is a defect — this milestone's third blocking
    anti-pattern is a number surviving beside a reason that was never checked, and it survived three
    documents last time.

    Record the reconciliation as a table in the SUMMARY: each number, every location it appears, and
    a confirmation that they agree.

    Update the ROADMAP Progress table's Phase 3 row to `Done`, and the Phase 3 detail block's
    criteria with the actual pattern count achieved, per D-16's requirement that the number is
    recorded after the fact. Scoped edits only — the other four phase blocks stay byte-identical.
  </action>
  <acceptance_criteria>
    - `cargo test --test perf_regression_test` passes.
    - The measured scan time is recorded in the SUMMARY with its headroom against the 200ms budget stated as a multiple, and the README figure matches it.
    - The Aho-Corasick row in `.planning/REQUIREMENTS.md` cites the newly measured figure rather than the stale one, with a note that the old number predates ENG-02 and this phase.
    - The reconciliation table in the SUMMARY covers at least: total pattern count, CAT-01 recall figures, the payload total of 12, the recall denominator of 72, and the clean corpus specimen count of 20.
    - Every number in that table is confirmed identical across all its listed locations; a mismatch blocks this task.
    - The README pattern-count sentence, the README category table, the catalogue's count line and `test_total_pattern_count` all state the same number.
    - `grep -c '^### Phase' .planning/ROADMAP.md` is still 5, and `git diff --stat .planning/ROADMAP.md` shows changes confined to the Phase 3 block and its Progress row.
    - `cargo test --locked` fully green.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked && test "$(grep -c '^### Phase' .planning/ROADMAP.md)" = "5"</automated>
  </verify>
  <done>The phase's performance cost is measured rather than assumed, and every number it publishes has been checked against every other place it appears.</done>
</task>

<task type="auto">
  <name>Task 3: File the deferrals and run the pre-PR gate</name>
  <files>docs/DETECTION-BACKLOG.md</files>
  <precondition>`gh` is authenticated against `UnityInFlow/injection-scanner` with issue-write scope. Probe it with a real command rather than trusting `gh auth status`; if issue creation fails, record the intended issue bodies in the SUMMARY and hand them to the developer rather than dropping them.</precondition>
  <read_first>
    - `.planning/phases/03-tool-permission-abuse-cat-01-33/03-CONTEXT.md` §"Deferred Ideas" — the five items, each with its stated reason.
    - `docs/DETECTION-BACKLOG.md` §Part 2 `PI050`-`PI059` — the section this phase implements, which needs its status updated and the dropped items recorded.
    - `patterns/core/exfiltration.yaml` around the pipe-to-shell pattern — the measured gap D-15 describes, so the issue can name the exact regex fragment and the exact input that does not match.
    - `.claude/skills/code-review/SKILL.md` and `.claude/skills/pr-artifacts/SKILL.md` — the project's pre-PR gate; per project convention there is no review hook, these skills are the gate.
    - `.claude/skills/pattern-library/SKILL.md` "Before you finish" checklist in full.
  </read_first>
  <action>
    File one issue per deferred item, each naming its reason from CONTEXT.md rather than restating
    the idea: the pipe-to-shell gap in the existing exfiltration pattern, with the exact regex
    fragment and the exact input shape that fails to match, tagged as its own sweep under GATE-04;
    the bare dangerous-command detector, noted as a distinct threat model needing its own
    false-positive story and a v0.3.0 backlog candidate; the backfill of the relaxed-control field
    across the existing 48 patterns, including the two known-slack v0.1.0 controls, deliberately
    outside this PR under GATE-04; and the two pre-existing catalogue sweep findings, which are a
    standing violation of the expect-empty rule that predates this milestone and has never had an
    issue. Leave the unclaimed frontmatter override keys as a note in the backlog rather than an
    issue — D-13 leaves them unclaimed on purpose and squatting an issue on them is the thing D-13
    avoids. Record every issue number in the SUMMARY.

    Update `docs/DETECTION-BACKLOG.md` §Part 2: mark the `PI050`-`PI059` section as shipped with its
    actual pattern count, and record which of its listed bullets were deliberately NOT implemented —
    the bare dangerous commands per D-14 and the pipe-to-shell item per D-15 — with a pointer to the
    issues just filed. A backlog that still lists a shipped item as pending is the same
    documentation-drift failure this milestone keeps finding.

    Then run the pre-PR gate. Invoke the project's `code-review` skill over the phase's changed
    source files, and the `pr-artifacts` skill, since project convention makes these the gate and
    there is no hook enforcing them. Walk the pattern-library skill's "Before you finish" checklist
    item by item and record each as satisfied or not; several are already covered by earlier plans,
    and confirming that is the point of walking it.

    Finish with the full gate: format check, clippy with warnings denied, and the locked test suite.
    Record the final test count against the 331 the milestone started at.
  </action>
  <acceptance_criteria>
    - At least four issues are filed and their numbers recorded in the SUMMARY, one per deferred item, each naming its CONTEXT.md reason.
    - The pipe-to-shell issue names the exact regex fragment and the exact input shape that does not match it.
    - `docs/DETECTION-BACKLOG.md` §Part 2's `PI050`-`PI059` section states the shipped pattern count and lists the deliberately unimplemented bullets with issue references.
    - The `code-review` skill has been run over the phase's changed source files and its findings are recorded as resolved or accepted with a reason.
    - The `pr-artifacts` skill has been run and its output recorded.
    - Every item on the pattern-library skill's "Before you finish" checklist is recorded as satisfied, with the plan and task that satisfied it.
    - `cargo fmt --all -- --check` passes.
    - `cargo clippy --all-targets --locked -- -D warnings` passes.
    - `cargo test --locked` passes, and the final test count is recorded against the 331 baseline.
    - `git log --oneline` for the phase shows the corpus and baseline commit strictly before the first commit containing a `PI05` pattern — D-04's ordering, verifiable in history rather than asserted.
    - `git log --merges` for the phase is empty — `main` stays strictly linear.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all-targets --locked -- -D warnings && cargo test --locked</automated>
  </verify>
  <done>Every deferral has an issue rather than a paragraph, the backlog no longer lists shipped work as pending, the project's own review skills have run, and the full gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real third-party documentation -> the release binary | The only place a pathological regex or a byte-slicing crash surfaces; fixtures are not real bytes |
| published numbers -> the reader | A security tool that publishes a recall figure is asking to be trusted on it; a figure that disagrees with its own test is worse than none |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-25 | Denial of Service | release binary crashing on real third-party bytes | high | mitigate | Sweep runs the release binary; any exit code beyond 0, 1 or 2 is treated as blocking rather than as a verdict (Task 1) |
| T-03-26 | Denial of Service | catastrophic backtracking in a new pattern | medium | mitigate | The sweep over real, long documents is where a pathological regex first shows as a timeout; a hang is triaged as blocking |
| T-03-27 | Repudiation | a published number disagreeing with its own test | high | mitigate | The reconciliation table covering every number and every location it appears (Task 2) |
| T-03-28 | Repudiation | a deferral recorded only as prose | medium | mitigate | One issue per deferred item with its reason, numbers recorded (Task 3) |
| T-03-29 | Tampering | a false positive triaged away rather than fixed | high | mitigate | Zero false positives permitted at merge; a narrowing or a drop plus a re-run compare is the only accepted resolution (Task 1) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed across the entire phase; `git diff --stat Cargo.toml Cargo.lock` empty |
</threat_model>

<verification>
- `cargo fmt --all -- --check`, `cargo clippy --all-targets --locked -- -D warnings`, `cargo test --locked` all green.
- The compare output is pasted into `03-SWEEP.md` with a verdict per finding.
- The self-scan returns at most the two known pre-existing findings.
- The reconciliation table shows agreement across every location.
- `git log --merges` empty for the phase; the corpus commit precedes the first `PI05` commit.
</verification>

<success_criteria>
GATE-03 satisfied as a recorded delta over a named directory set with the release binary; the repo
self-scan clean beyond the two known pre-existing findings; every published number reconciled; every
deferral filed as an issue; the project's review skills run; the full gate green and the history
still linear.
</success_criteria>

## Artifacts this phase produces

This plan fills the after-sweep section of
`.planning/phases/03-tool-permission-abuse-cat-01-33/03-SWEEP.md`, updates
`docs/DETECTION-BACKLOG.md` §Part 2, corrects the stale headroom row in `.planning/REQUIREMENTS.md`,
closes out `.planning/ROADMAP.md`'s Phase 3 status, and files the deferral issues. The complete
phase-wide artifact table is in `03-01-PLAN.md`.

<output>
Create `.planning/phases/03-tool-permission-abuse-cat-01-33/03-07-SUMMARY.md` when done, recording
the sweep delta with a verdict per finding, the self-scan result, the reconciliation table, the
measured scan time and headroom, the filed issue numbers, and the final test count.
</output>
