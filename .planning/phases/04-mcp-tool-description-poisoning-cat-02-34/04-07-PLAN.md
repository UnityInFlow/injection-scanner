---
phase: 04-mcp-tool-description-poisoning-cat-02-34
plan: 07
type: execute
wave: 6
depends_on: [04-06]
files_modified:
  - .planning/phases/04-mcp-tool-description-poisoning-cat-02-34/sweep-final-2026-09-03/
  - .planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-SWEEP.md
  - .planning/phases/04-mcp-tool-description-poisoning-cat-02-34/deferred-items.md
  - CHANGELOG.md
  - README.md
  - PATTERNS.md
  - docs/DETECTION-BACKLOG.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [CAT-02, GATE-01, GATE-02, GATE-03, GATE-04, GATE-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every number this phase publishes — pattern count, recall, corpus totals, test count — is measured on the finished tree and agrees across `EXPECTED`, the README, `PATTERNS.md` and the catalogue."
    - "The full ten-pattern category has an adjudicated two-directional GATE-03 delta against the pre-edit baseline, run on the finished tree."
    - "Every limitation this phase measured has either a fix or a filed issue: the JSONC parse gap, the decoder's absence from the structural pass, D-05's structural cross-reference, and D-01's third-person blind spot."
    - "The PR contains one category and only one category (GATE-04); CAT-03 is untouched."
    - "Phase 5's handoff does not depend on planning artifacts that a rebase can delete."
  artifacts:
    - "sweep-final-2026-09-03/ and the completed 04-SWEEP.md with its reconciliation table"
    - "deferred-items.md — every deferral with its filed issue number"
    - "CHANGELOG.md entry; docs/DETECTION-BACKLOG.md CAT-02 bullets marked shipped"
    - "ROADMAP, REQUIREMENTS and STATE updated to the measured position"
  key_links:
    - "pre-edit baseline (04-01) -> final sweep -> the whole-category evidence the PR reports"
    - "measured limitations -> filed issues -> the v0.3.0 backlog"
    - "STATE.md -> the Phase 5 planner, which inherits whatever is written there and nothing else"
---

<objective>
Close CAT-02: reconcile every published number against the finished tree, run the whole-category
GATE-03 delta, convert every measured limitation into a filed issue, and leave Phase 5 a handoff that
does not depend on a directory a rebase can delete.

Purpose: Phase 3 shipped and then spent a quick task and a review cycle correcting a category row and
a set of numbers that had drifted between plans. This plan makes the reconciliation a step rather
than a follow-up. It also discharges the four limitations this phase measured rather than guessed —
each is real, each is silent, and a silent limitation in a security tool is a claim the repository
makes without knowing it.

Output: a reconciled set of published numbers, a committed whole-category sweep, filed issues for
every deferral, and updated tracking.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-CONTEXT.md
@.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-RESEARCH.md
@.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-SWEEP.md
@.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-04-SUMMARY.md
@.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-05-SUMMARY.md
@.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-06-SUMMARY.md

@README.md
@PATTERNS.md
@CHANGELOG.md
@docs/DETECTION-BACKLOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The whole-category sweep and the number reconciliation</name>
  <files>.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/sweep-final-2026-09-03/, .planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-SWEEP.md, README.md, PATTERNS.md</files>
  <read_first>
    - `04-SWEEP.md` in full — the recorded directory list, the baseline totals, and all three per-plan deltas with their adjudications.
    - `README.md` §"Pattern Categories" and §"How Much Does It Actually Catch?" — every number this phase touched.
    - `PATTERNS.md` §Categories — the row added in plan 04-04.
    - `tests/recall_test.rs` `EXPECTED` — the two CAT-02 rows and the running commentary above them.
    - The three per-plan SUMMARYs — each records numbers that must now agree with the tree.
  </read_first>
  <action>
    Build the release binary from the finished tree and run the sweep one final time over the SAME
    directory list recorded in `04-SWEEP.md`, into a final output directory. Compare in both
    directions against the plan 04-01 pre-edit baseline. The per-plan deltas isolated each arm; this
    one is the whole-category measurement the PR reports.

    Adjudicate anything that appears here and did not appear in a per-plan delta. There should be
    nothing. If there is, it means a re-narrowing in a later plan changed an earlier pattern's
    behaviour, or the third-party corpus itself shifted between runs. Either explanation must be
    written down; neither is acceptable as an unexplained difference.

    Then reconcile every published number against the finished tree, by measuring rather than by
    copying from a SUMMARY:

    - the total pattern count from the loader, against the test that pins it, the README's
      pattern-count sentence, and the README Pattern Categories table's per-category counts;
    - the recall numbers from a fresh run, against `EXPECTED`, its per-row commentary, and the README
      recall table including its total row's numerator and denominator;
    - the corpus totals — twelve CAT-02 payloads across the two rows — against any count stated in
      the README or the corpus READMEs;
    - the test count from `cargo test --locked`, against the count recorded in plan 04-01;
    - the `PATTERNS.md` Categories row's severity description, against the severity fields actually
      present in the category file.

    Where two places disagree, the tree is right and the document is corrected — never the reverse.
    List every correction made, because a number needing correction at close-out says something about
    where the process leaked and Phase 5 should see it.

    Run the whole-repo self-scan a final time and confirm the set outside `examples/`, `patterns/`,
    `tests/` and `tools/` still matches plan 04-01's recorded baseline: the two known standing
    catalogue self-matches and nothing else. A third entry is a false positive this phase introduced
    on this repository's own prose, and it is fixed here rather than filed.

    Append the final sweep and the reconciliation table to `04-SWEEP.md` and commit the sweep output
    directory.
  </action>
  <acceptance_criteria>
    - The final sweep output directory exists with a manifest whose swept directory list is identical to the baseline manifest's.
    - Both comparison directions against the pre-edit baseline are recorded verbatim in `04-SWEEP.md`, with every entry adjudicated and none left unexplained.
    - `04-SWEEP.md` carries a reconciliation table with one row per published number, each giving the measured value and the documents it was checked against.
    - `cargo test --locked` is fully green.
    - `cargo test --test catalogue_test` passes without regenerating anything — the catalogue was already current when this task began.
    - The README's pattern-count sentence, the README Pattern Categories table's counts, and the loader's actual pattern count all agree.
    - The README recall table's total numerator equals the sum of its rows, and its denominator reads 84.
    - The `PATTERNS.md` Categories row's severity description matches the severity fields present in the category file.
    - The self-scan set outside `examples/`, `patterns/`, `tests/` and `tools/` is identical to plan 04-01's recorded baseline.
    - Every correction made during reconciliation is listed in the plan SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked && cargo fmt --all -- --check && cargo clippy --all-targets --locked -- -D warnings && bash scripts/gate03-sweep.sh --compare .planning/phases/04-mcp-tool-description-poisoning-cat-02-34/sweep-baseline-2026-09-03 .planning/phases/04-mcp-tool-description-poisoning-cat-02-34/sweep-final-2026-09-03</automated>
  </verify>
  <done>The full category is measured against roughly 1,300 real third-party files in both directions on the finished tree, and every number this repository publishes about CAT-02 has been checked against the tree that produces it rather than against the document that last stated it.</done>
</task>

<task type="auto">
  <name>Task 2: Turn every measured limitation into a filed issue, and write the consumer-facing record</name>
  <files>.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/deferred-items.md, CHANGELOG.md, docs/DETECTION-BACKLOG.md, README.md</files>
  <read_first>
    - `04-RESEARCH.md` §Q1's JSONC finding and §"Open Questions" — both the gap and the research's own recommendation that it be filed rather than folded in.
    - `04-05-SUMMARY.md` — whether the encoded-description payload was reached, which determines how the decoder gap is worded.
    - `04-CONTEXT.md` §Deferred Ideas and its carried-over Phase 3 follow-ups.
    - `04-03-SUMMARY.md` — any registry candidate rejected because a pre-existing pattern fired on it.
    - `CHANGELOG.md`'s most recent entry — the Added/Changed/Security subsection structure and bullet phrasing to match.
    - `docs/DETECTION-BACKLOG.md` §`PI060`-`PI069` — the four bullets this phase answers.
  </read_first>
  <action>
    File an issue for each measured limitation, and record its number. An unfiled limitation is
    indistinguishable from an unknown one after this branch merges.

    - The JSONC parse gap. A comment-bearing `mcp.json` is rejected by the JSON parser, the structural
      pass is skipped for that document under the skip-do-not-abort rule, and the scan reports zero
      with no diagnostic on stdout or stderr. It affects a real, current host family. The issue must
      carry the reproduction and state that the silence is the dangerous part.
    - The decoder's absence from the structural pass. The decoded-layer pass skips any pattern whose
      scope is not prose, so an encoded value inside parsed configuration is unreachable to a
      structural pattern. Word this from what plan 04-05 measured: if the prose arms reached the
      encoded-description payload, the issue is about structural patterns specifically; if they did
      not, it is broader and the issue must say so.
    - D-05's structural cross-reference for tool shadowing — resolving whether a referenced tool
      actually exists among the manifest's declared tools. Cross-node state in the projection pass,
      engine-scale, explicitly deferred by CONTEXT.
    - Any registry candidate plan 04-03 rejected because a shipped pattern fired on it. That is a
      genuine false positive on a real third-party manifest, out of scope here only because GATE-04
      forbids touching another category in this PR.

    Do NOT file new issues for the carried-over Phase 3 follow-ups — WR-02, WR-03 and the two
    pre-existing catalogue self-matches. Record them in the deferred-items file as still open with
    their existing status, so they are visible without being duplicated.

    Write `deferred-items.md` listing every deferral with its issue number, why it was deferred, and
    what would trigger it. D-01's accepted third-person blind spot goes here too, cross-referencing
    where plans 04-05 and 04-06 recorded it in shipped files.

    Mark the CAT-02 bullets in `docs/DETECTION-BACKLOG.md` as shipped, in the register Phase 3 used
    for its own. Where a bullet is only partially answered — the rug-pull bullet is, since only the
    conditional language is detectable — say which part shipped and which is bounded, and point at
    the pattern comment that states the bound.

    Write the CHANGELOG entry in the existing structure: the ten new pattern ids with a one-line
    summary each, the new category, the severity banding and what it means for a consumer's CI, and
    a security note naming the limitations just filed. A consumer reading only the changelog should
    learn both what newly fires and what still does not.

    Confirm the README's behaviour-change callout, written across plans 04-04 and 04-05, states the
    complete picture for an upgrading consumer: which findings are MEDIUM and which are HIGH, that
    HIGH is what `install-hook` blocks at, that the baseline flag is the migration path, and what
    this category deliberately does not detect.
  </action>
  <acceptance_criteria>
    - `deferred-items.md` exists and every entry carries either an issue number or an explicit note that it is an existing open item rather than a new one.
    - Each newly filed issue is reachable and its number appears in `deferred-items.md`.
    - `grep -c 'PI060' CHANGELOG.md` is at least 1, and the entry names all ten ids.
    - `docs/DETECTION-BACKLOG.md`'s CAT-02 bullets are marked shipped, with the partially-answered bullet distinguished from the fully-answered ones.
    - The README behaviour-change callout names the MEDIUM band, the HIGH band, the `install-hook` threshold, the baseline migration path, and at least one thing the category deliberately does not detect.
    - `cargo test --locked` is green — the docs edits did not disturb any pinned count.
    - The whole-repo self-scan set outside `examples/`, `patterns/`, `tests/` and `tools/` is unchanged; the CHANGELOG and backlog edits describe payload shapes without quoting them verbatim, since this repository scans itself.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --locked && cargo run --release -- check . --exclude '.planning/**' --format json | python3 -c "import json,sys; rows=[(r['file'],m['line'],m['pattern_id']) for r in json.load(sys.stdin) for m in r['matches'] if not any(k in r['file'] for k in ('examples/','patterns/','tests/','tools/'))]; print(rows); sys.exit(0 if len(rows)==2 else 1)"</automated>
  </verify>
  <done>Four measured limitations are filed issues rather than tacit knowledge, the backlog reflects what actually shipped and what is only bounded, and a consumer reading the changelog learns both what newly fires and what still does not.</done>
</task>

<task type="auto">
  <name>Task 3: Tracking close-out, GATE-04 scope check, and the Phase 5 handoff</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 4 and §Progress — the plan checklist and the status table.
    - `.planning/REQUIREMENTS.md` — the CAT-02 checkbox and the coverage table.
    - `.planning/STATE.md` — the Current Phase section, the phases table, the recall table, the gates table and the Session Notes register.
    - `.planning/.continue-here.md` — the standing anti-pattern table, which this phase has evidence to extend.
    - The three per-plan SUMMARYs plus `04-SWEEP.md` — the measured record everything here must agree with.
  </read_first>
  <action>
    Update the tracking documents to the measured position, then verify the PR's scope, then leave
    the handoff.

    Mark Phase 4 complete in the ROADMAP with its plan checklist ticked, update the Progress table,
    and correct the milestone's library and recall projections to what actually shipped rather than
    what was projected when the milestone opened. Tick CAT-02 in REQUIREMENTS and update its coverage
    table row.

    Update STATE: the current phase, the phases table, the recall table with the measured per-category
    numbers, the progress counters, and a Session Notes entry in the existing register. The Session
    Notes entry is the highest-value artifact this plan produces, because it is what the Phase 5
    planner reads. Record what was measured rather than assumed, specifically: that the
    `relaxed_pattern` ratchet's id range did not cover this phase's ids despite an adjacent comment
    claiming it did; that the structural corpus collector and both clean-corpus enumerations are
    non-recursive, so a subdirectory is invisible to them; that the decoded-layer pass is restricted
    to prose-scoped patterns; and the narrowing measurements that decided the config-hygiene and
    description-poisoning arms. Carry forward, explicitly, what Phase 5 inherits: the `relaxed_pattern`
    obligation, the CR-01 negation rule, the per-category structural corpus layout, and the open
    follow-ups.

    Add a row to `.planning/.continue-here.md`'s anti-pattern table if this phase produced a new one.
    A comment that asserts a mechanism's scope while the code disagrees is a strong candidate and is
    a variant of the inheriting-a-documented-reason failure already listed; record it in whichever
    form the evidence supports.

    Verify GATE-04 before the PR. Diff the whole branch against its base and confirm no file relating
    to the next category was touched, that the only pattern file added is this category's, that no
    existing category's YAML changed, and that the dependency manifests are untouched. One category
    per PR is the gate that keeps a false-positive blast radius reviewable, and it is checked by
    looking at the diff, not by remembering the intent.

    Restore the Phase 3 planning directory. It is present at commit `752ac98` and absent from `main`:
    the seven plans, seven summaries, the review, the sweep evidence and the CR-01 quick-task
    directory were all lost when Phase 3's branch was rebase-merged. `04-CONTEXT.md` cites two of
    those files as canonical references, so the citations currently point at nothing. Restore them
    with `git checkout 752ac98 -- <paths>` in a documentation-only commit, or — if the developer
    prefers not to carry them on `main` — record where they live and correct the citations in
    `04-CONTEXT.md`. Either way the dangling references must stop dangling, and the loss must be
    noted in STATE so the same rebase does not silently do it again to this phase.

    Finally, assemble the developer review packet and record it in the plan SUMMARY so the
    end-of-phase verification step has everything it needs in one place: the final recall numbers
    against the pre-phase numbers; the total pattern count; the whole-category GATE-03 delta with its
    adjudication summary; the corrections the reconciliation required; the filed issue numbers; the
    GATE-04 diff check result; and the two accepted costs this phase records in shipped files —
    D-01's third-person blindness and the rug-pull bound. Three questions need a human answer before
    a PR is opened, and they are carried in this task's human check rather than resolved here.
  </action>
  <acceptance_criteria>
    - ROADMAP Phase 4 is marked complete with its plan checklist ticked and the Progress table updated.
    - REQUIREMENTS' CAT-02 checkbox is ticked and its coverage table row updated.
    - STATE's current phase, phases table, recall table and progress counters match the measured tree.
    - STATE carries a Session Notes entry naming all four measurements listed in the action.
    - `git diff --name-only <base>..HEAD` contains exactly one added file under `patterns/core/`, and no other file under `patterns/core/` is modified.
    - `git diff --name-only <base>..HEAD` contains no path relating to the next category.
    - `git diff --stat <base>..HEAD -- Cargo.toml Cargo.lock` is empty.
    - The `04-CONTEXT.md` citations to Phase 3 artifacts resolve — either the files are restored or the citations name where they live.
    - The plan SUMMARY contains the developer review packet with all six items listed in the action.
  </acceptance_criteria>
  <verify>
    <human-check>
      Review the packet recorded in `04-07-SUMMARY.md` — recall delta, pattern count, whole-category
      GATE-03 adjudication summary, reconciliation corrections, filed issue numbers, GATE-04 diff
      check result, and the two accepted costs — then answer three questions before a PR is opened:
      1. Is the severity banding right — three arms at HIGH, the severity `install-hook` blocks
         commits at, and seven at MEDIUM?
      2. Are D-01's third-person blind spot and the rug-pull bound the right trades, given the
         false-positive evidence recorded behind them?
      3. Should the Phase 3 planning artifacts be restored to `main`, or referenced where they live
         at commit `752ac98`?
    </human-check>
    <automated>cargo test --locked && cargo fmt --all -- --check && cargo clippy --all-targets --locked -- -D warnings && git diff --stat origin/main..HEAD -- Cargo.toml Cargo.lock | wc -l | grep -qx '0'</automated>
  </verify>
  <done>Tracking matches the measured tree, GATE-04 is verified against the diff rather than against intent, the Phase 5 handoff records what was measured rather than what was assumed, the dangling Phase 3 citations are resolved, and the developer has signed off on the severity banding and the two accepted costs before any PR exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published numbers -> users and consumers | The recall figure and the pattern count are the tool's central public claims; drift between the tree and the README is a false claim in the repository's own voice |
| PR diff -> reviewer | GATE-04 exists because a multi-category diff has an unreviewable false-positive blast radius |
| STATE.md -> the Phase 5 planner | Everything not written here is lost at the next context boundary, and Phase 3's own artifacts were lost at a rebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-34 | Spoofing | a published number drifting from the tree | high | mitigate | Task 1 reconciles every number by measuring, with a per-number table recording what it was checked against |
| T-04-35 | Repudiation | a measured limitation surviving only as tacit knowledge | high | mitigate | Task 2 files an issue per limitation and records the numbers in a committed deferrals file |
| T-04-36 | Elevation of Privilege | a second category slipping into the PR | high | mitigate | Task 3 verifies GATE-04 against the branch diff, not against intent |
| T-04-37 | Repudiation | planning artifacts lost to a rebase, leaving citations dangling | medium | mitigate | Task 3 restores the Phase 3 directory or corrects the citations, and records the loss mode in STATE |
| T-04-38 | Spoofing | changelog or backlog prose quoting payload text verbatim into a self-scanned file | medium | mitigate | Task 2's automated check asserts the self-scan set outside the four excluded directories is exactly the two known standing entries |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed anywhere in this phase; the empty dependency-manifest diff is a GATE-04 criterion in Task 3 |
</threat_model>

<verification>
- `cargo fmt --all -- --check`, `cargo clippy --all-targets --locked -- -D warnings`, `cargo test --locked` green.
- Final two-directional GATE-03 comparison recorded and fully adjudicated.
- Reconciliation table complete, with every correction listed.
- Self-scan set outside `examples/`, `patterns/`, `tests/` and `tools/` is exactly the two known standing entries.
- Every deferral carries an issue number or an explicit existing-item note.
- GATE-04 verified against the branch diff.
- Developer checkpoint answered and recorded.

</verification>

<success_criteria>
CAT-02 is closed with every published number measured and agreeing across four documents, a
whole-category two-directional sweep adjudicated entry by entry, four measured limitations converted
into filed issues, tracking updated to the real position, GATE-04 verified from the diff, the
dangling Phase 3 citations resolved, and the developer signed off on the severity banding and the two
accepted costs.
</success_criteria>

<output>
Create `.planning/phases/04-mcp-tool-description-poisoning-cat-02-34/04-07-SUMMARY.md` when done,
recording the final numbers, the reconciliation corrections, the whole-category GATE-03 adjudication,
the filed issue numbers, the GATE-04 diff check result, and the developer's answers to the three
review questions.
</output>
